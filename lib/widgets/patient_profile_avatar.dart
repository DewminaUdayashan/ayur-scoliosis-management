import 'package:flutter/material.dart';

class PatientProfileAvatar extends StatelessWidget {
  const PatientProfileAvatar({super.key});

  @override
  Widget build(BuildContext context) {
    return CircleAvatar();
  }
}
